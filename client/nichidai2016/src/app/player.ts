export class Player {
  private rank: number;
  private name: string;
  private time: string;
  constructor(rank: number, name: string, time: string) {
    this.rank = rank;
    this.name = name;
    this.time = time;
  }
}
